pipeline {
   agent any
   stages {
      stage('kubectl commands') {
         steps {
             script{
                 echo ${select}
                 cat test.yml
		        }

              }
            }    
        }
    }
