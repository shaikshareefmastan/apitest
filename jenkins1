pipeline {
   agent any
   stages {
      stage('kubectl commands') {
         steps {
             script{
                 echo ${env.select}
                 cat test.yml
		        }

              }
            }    
        }
    }
