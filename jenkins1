pipeline {
   agent any
   stages {
      stage('kubectl commands') {
         steps {
             script{
                 sh "echo ${params.select}"
                 "sh cat test.yml"
		        }

              }
            }    
        }
    }
