pipeline {
   agent any
   stages {
      stage('kubectl commands') {
         steps {
             script{
                 sh "echo ${select}"
                 "sh cat test.yml"
		        }

              }
            }    
        }
    }
