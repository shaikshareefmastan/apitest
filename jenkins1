pipeline {
   agent any
   stages {
      stage('kubectl commands') {
         steps {
             script{
	          if (params.deploy) {
                sh("echo ${select}")
              }

              }
            }    
        }
    }
}
