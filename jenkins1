pipeline {
   agent any
   stages {
      stage('kubectl commands') {
         steps {
             script{
                 echo ${params.select}
                 cat test.yml
		        }

              }
            }    
        }
    }
