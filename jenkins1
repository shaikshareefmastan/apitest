pipeline {
   agent any
   stages {
      stage('kubectl commands') {
         steps {
             script{
	          if (params.deploy) {
                sh("echo ${select}")
              } else {
               sh ("cat test.yml")
              }
		        }

              }
            }    
        }
    }
